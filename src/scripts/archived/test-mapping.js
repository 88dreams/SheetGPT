// Simple script to run the mapping tests
// Run this with: node test-mapping.js

// Import the test runner
const { runAllTests } = require('./frontend/src/components/data/SportDataMapper/utils/test-mapping');

// Run all the tests
runAllTests();